# Java Templates

Templates for Java-based projects.

## Contents
- **application**: Standard Java SE application structure.
- **web_server**: Boilerplate for a dependency-free HTTP server built on the JDK's `java.net.ServerSocket` and a thread pool (no external framework).

## Usage
Used by the Coder sub-agent when `java` is specified as the target language.
