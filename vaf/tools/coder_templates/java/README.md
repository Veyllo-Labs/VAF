# Java Templates

Templates for Java-based projects.

## Contents
- **application**: Standard Java SE application structure.
- **web_server**: Boilerplate for a Java web server (e.g., Spring Boot or Jetty based).

## Usage
Used by the Coder sub-agent when `java` is specified as the target language.
